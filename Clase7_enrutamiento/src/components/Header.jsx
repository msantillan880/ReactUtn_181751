const Header = () => {
  return (
    <header className="app__header">
      <p className="app__eyebrow">Modulo 2 - Unidad 3</p>
      <h1>Mini dashboard de enrutamiento</h1>
      <p className="app__description">
        Rutas publicas y protegidas con BrowserRouter, Routes, Route, useParams,
        useSearchParams, useNavigate, Navigate y useLocation.
      </p>
    </header>
  )
}

export { Header }