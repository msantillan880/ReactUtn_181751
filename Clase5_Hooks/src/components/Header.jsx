const Header = () => {
  return (
    <header className="header">
      <h1>Gestor de Tareas con Hooks</h1>
      <p>useState, useEffect, useRef, useMemo, useCallback y custom hook useLocalStorage.</p>
    </header>
  )
}

export { Header }