const Header = () => {
  return (
    <header className="app__header">
      <p className="app__eyebrow">Modulo 2 - Unidad 2</p>
      <h1>Listado de usuarios en API REST</h1>
      <p className="app__description">
        Ejemplo de consumo de API con fetch, useEffect y renderizado condicional
        en React.
      </p>
    </header>
  )
}

export { Header }